server:
  port: 0
spring:
  application:
    name: driver
  cloud:
    config:
      uri: http://localhost:8899
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
#  instance:
#    instance-id: ${spring.application.name}-${random.int}
#    hostname: localhost