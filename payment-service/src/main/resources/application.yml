server:
  port: 9193
  error:
    include-message: always
spring:
  datasource:
    username: root
    password: 1234
    url: jdbc:mysql://localhost:3306/vehiclepasser_payment?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: none

